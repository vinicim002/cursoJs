const calc = (x, y) => {
    const soma = x + y;
    const subtracao = x - y;
    const multiplicacao = x * y;
    const divisao = x / y; 

    console.log(`Soma = ${soma}\nSubtracao = ${subtracao}\nMultiplicacao = ${multiplicacao}\nDivisao = ${divisao}`);
}

calc(2,2);